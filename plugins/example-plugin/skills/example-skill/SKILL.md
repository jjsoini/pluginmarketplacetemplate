---
name: example-skill
description: Reference skill that documents a simple repeatable team workflow. Use when the user wants to see how a plugin skill is structured, or asks to run the example workflow.
---

# Example Skill

This skill is a template. Replace its contents with a real, repeatable workflow your team performs often.

## When to use

Invoke this skill when the user asks to run the example workflow, or when you want to demonstrate how a packaged skill behaves.

## Steps

1. Confirm with the user what they want to accomplish.
2. Walk through the workflow steps (replace these with your real procedure).
3. Summarize what was done.

## Notes

- A skill can bundle additional files (scripts, templates, reference docs) alongside this `SKILL.md`. Reference them with relative paths.
- Keep the `description` in the frontmatter specific — it is how Claude decides when to load this skill.
