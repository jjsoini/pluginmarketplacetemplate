---
description: Greet the user and confirm the example plugin is installed.
argument-hint: "[name]"
---

You are running the `/hello` command from the example-plugin.

Greet the user warmly. If an argument was provided, address them by that name: "$ARGUMENTS".

Then briefly confirm that the example plugin is installed and working, and point them to the plugin's README for how to build their own command.
